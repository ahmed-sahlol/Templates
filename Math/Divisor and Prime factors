vector<long long> divisor(long long a) {
    vector<long long> v;
    for (long long i = 1; i * i <= a; ++i) {
        if (a % i == 0) {
            v.push_back(i);
            if (i * i != a) v.push_back(a / i);
        }
    }
    return v;
}
